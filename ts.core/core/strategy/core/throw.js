module.exports = function (options, callback, errorcallback) {
    console.log('lets throw error');
    options = options || { errormessage: 'throw error from api core' };

    if (options) { 
        opt.tostring();
    }
}